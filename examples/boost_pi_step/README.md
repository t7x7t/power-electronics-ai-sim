# Boost L1 reference run

```powershell
pe-sim run examples/boost_pi_step/config.json --backend boost
```

This is a self-owned ideal averaged L1 reference model. It is for adapter and
runtime verification, not a switching, thermal, hardware, or product model.
