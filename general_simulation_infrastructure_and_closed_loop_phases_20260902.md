# 通用电力电子仿真工程：总体目标与架构指导

**日期**：2026-09-03  
**文档性质**：项目总体目标、架构原则和建设方向  
**适用范围**：基于 PySpice、Ngspice 和 AI Agent 的通用电力电子仿真参考工程

---

## 1. 文档定位

本文是后续工程建设和内容取舍的**总目标指引文档**。它只描述：

- 项目为什么建立；
- 项目希望提供什么；
- 各类工作应如何分层；
- 不同组件之间应保持什么职责边界；
- 如何保证工程能够扩展、复现、审计和维护；
- AI Agent 应当在什么原则下参与工作。

本文不规定某一种具体电路、控制算法、参数、实验命令、数据字段、文件接口或实现方式。具体技术内容应当放在独立的参考样例、专题文档或用户自己的扩展工程中。

---

## 2. 项目根本目的

本项目不是一个面向特定电源产品的开箱即用仿真工程，也不是一套替用户完成电路设计和控制器设计的自动化系统。

本项目希望提供的是：

```text
一套电力电子仿真基础设施的建设范式
一套借助 AI Agent 开展仿真工作的组织方法
一套具体电路和控制器接入的参考思路
一套运行、数据导出、可视化和结果审计的通用方法
一套避免实验代码失控和研究结果失真的工程约束
```

使用者应当在此基础上，根据自己的研究目标自行完成：

- 系统需求定义；
- 电路和模型设计；
- 控制算法设计；
- 实验方案设计；
- 结果解释和工程判断。

基础设施的作用不是替代这些工作，而是为这些工作提供稳定、清晰、可追溯的承载方式。

---

## 3. 项目不追求的目标

项目不以以下内容为目标：

- 提供适用于所有拓扑和功率等级的统一成品模型；
- 提供无需理解系统即可直接使用的控制器；
- 提供自动产生可信工程结论的 AI 系统；
- 用通用模板替代具体电路的工程设计；
- 用仿真结果替代器件、硬件、实时性或安全验证；
- 用单一性能指标代表完整的控制质量；
- 将某个具体研究案例包装成普遍适用的结论。

项目中的任何具体电路、算法或实验，都只能作为说明基础设施使用方式的参考内容。

---

## 4. 总体架构思想

项目采用“稳定基础、通用运行、专项扩展、独立证据”的总体思想：

```text
基础设施
    -> 提供稳定的仿真承载能力

通用运行框架
    -> 规定一次仿真如何被组织和执行

专项研究内容
    -> 表达使用者想研究的电路、控制器和问题

结果与证据体系
    -> 记录运行事实并限定可以得出的结论
```

这几个部分应当相互协作，但不能相互替代：

- 基础设施不应被某种算法的特殊需求绑死；
- 通用运行框架不应包含某种研究问题的结论；
- 专项研究内容不应破坏公共运行边界；
- 结果体系不应为了得到更好的指标而修改事实；
- AI Agent 不应越过职责边界自动作出工程判断。

---

## 5. 基础设施层

基础设施层是项目中复用时间最长、变化频率最低的部分。它的职责是建立一个能够承载不同电路、不同控制器和不同实验方式的公共底座。

基础设施层应当关注以下性质：

- 可配置；
- 可扩展；
- 可替换；
- 可测试；
- 可追溯；
- 可审计；
- 可恢复；
- 与具体研究问题解耦。

基础设施层不应直接承载某个具体研究项目的临时逻辑，也不应因为某一次实验失败就不断增加难以复用的特殊分支。

### 5.1 基础设施的组成方向

基础设施可以从以下方面建设：

- 环境和依赖管理；
- 项目与模块组织；
- 模型和电路的承载方式；
- 控制器的接入方式；
- 时间、信号和数据的组织方式；
- 安全边界和运行约束；
- 配置、版本和内容识别；
- 测试、检查和故障处理；
- 结果保存和外部工具连接。

这些内容只应规定能力、边界和责任，不应在总目标文档中规定某种具体实现。

### 5.2 基础设施的建设标准

基础设施的每一项能力都应当能够回答：

- 它服务于哪些类型的工作？
- 它不应承担哪些工作？
- 它是否支持替换和扩展？
- 它如何被验证？
- 它发生变化时如何被识别？
- 它如何避免影响已有研究？

---

## 6. 通用仿真运行层

通用运行层负责为每一次仿真提供一致的运行秩序，而不是规定仿真的具体研究内容。

它应当保证：

- 使用者能够明确指定本次运行的对象和目的；
- 运行前能够确认所使用的配置和版本；
- 运行过程中各组件按照清晰边界协作；
- 运行结束后能够区分完成、失败、不合格和未完成；
- 运行事实能够被保存和再次检查；
- 不同类型的研究可以共享同一运行基础。

通用运行层应当支持不同的运行节奏、不同的控制状态、不同的实验对象和不同的结果规模，而不预先假定所有仿真都具有相同的结构。

通用运行层只负责运行秩序，不负责替使用者定义研究结论。

---

## 7. 模型、电路与控制器的职责边界

模型、电路和控制器是可以由使用者自行实现和替换的专项内容。基础设施只提供它们接入公共运行体系所需要的边界和约束。

应当保持以下基本关系：

```text
模型或电路
    表达被研究对象

控制器
    根据允许获得的信息产生控制意图

运行框架
    负责安排两者之间的交互

约束与审计机制
    负责检查交互是否符合规定
```

模型内部用于生成响应的信息，不应在没有明确授权和记录的情况下成为控制器的隐含信息来源。

控制器的研究自由应当得到支持，但这种自由不能通过绕过测量边界、时间边界、执行边界和安全边界来实现。

---

## 8. 面向多种控制方法的兼容性原则

基础设施不应围绕某一种控制方法设计。它应当允许使用者接入：

- 传统反馈方法；
- 前馈和预览方法；
- 反馈与前馈组合方法；
- 事件型和重复型方法；
- 优化和约束型方法；
- 在线辨识和在线学习方法；
- 机器学习和神经网络方法；
- 使用者未来提出的其他方法。

兼容性不是为每种方法预先编写特殊逻辑，而是通过稳定的抽象和清晰的扩展边界，让新的方法能够在不破坏公共能力的前提下接入。

基础设施不应预先假定：

- 所有控制器具有相同的输入；
- 所有控制器具有相同的状态；
- 所有控制器以相同频率运行；
- 所有控制器的输出形式相同；
- 所有控制器都不需要历史信息；
- 所有研究都具有事件；
- 所有研究都需要学习；
- 所有结果都适合用同一套指标描述。

对于复杂控制器，基础设施应优先提供可组合的能力，而不是不断增加针对某个算法的专用分支。

---

## 9. 结果、数据和证据体系

结果处理属于通用仿真工程的重要组成部分，不属于某一种事件型控制或前馈算法的专属功能。

结果体系的根本目的不是生成更多文件，而是保证：

- 运行事实不会被遗忘；
- 结果来源能够追溯；
- 不同结果能够判断是否可比；
- 失败和不合格运行不会被误当作成功；
- 数据处理过程不会改变结论边界；
- 图形、指标和报告能够回到原始运行；
- 研究结论不会超出实际证据范围。

结果体系应当将以下概念分开：

```text
运行是否完成
数据是否有效
实验是否合格
结果是否可比较
是否允许用于学习
是否足以支持某种结论
```

结果导出、可视化、汇总和报告都应建立在这一分离基础上。

---

## 10. 版本识别、Hash 与基准保护

在 AI Agent 参与的开发和调试过程中，模型、电路、配置、参数和数据可能被修改。项目需要能够识别这些变化，并防止修改后的内容被误认为原始基准。

Hash 和版本识别机制的根本目的在于：

- 发现内容变化；
- 建立内容身份；
- 支持结果追溯；
- 防止不同版本被混淆；
- 保护参考基准不被无记录覆盖；
- 让 AI Agent 的修改可被检查和回滚。

这类机制不是为了阻止所有修改，也不能替代权限控制、代码审查和工程师判断。合理的工作方式应当允许：

```text
探索性修改
  -> 识别变化
  -> 形成新版本
  -> 人工确认
  -> 进入正式实验
```

参考内容应当能够被识别、被比较、被恢复，而不是依赖使用者记忆其是否被改动。

---

## 11. 安全、资格与研究边界

安全检查、实验资格和研究结论是三个不同层次的问题。

- **安全**关注运行是否违反预定边界；
- **资格**关注数据是否适合进入某项统计、比较或学习；
- **结论**关注现有证据能够支持什么程度的判断。

三者不能被一个简单的成功或失败状态替代。

项目应当允许不同研究类型定义自己的资格要求，但这些要求必须建立在统一的公共边界之上。特殊研究的资格规则应当作为扩展内容存在，而不应改变整个工程对运行事实和安全边界的基本认识。

任何仿真结果都必须明确其适用范围、模型依赖和证据等级。仿真研究结果不能自动转化为真实器件、硬件、实时性或产品安全结论。

---

## 12. AI Agent 协作原则

AI Agent 是项目参与者和辅助工具，不是自动承担工程责任的主体。

AI Agent 可以帮助使用者：

- 理解需求和现有结构；
- 组织模型、电路、控制器和实验内容；
- 设计和实现参考代码；
- 编写和执行验证工作；
- 分析运行结果和失败现象；
- 整理数据、图形和文档；
- 检查版本、边界和可复现性。

AI Agent 不应未经确认决定：

- 具体系统的工程额定值；
- 真实器件的安全边界；
- 研究结论的适用范围；
- 仿真结果是否代表硬件；
- 第三方资料的发布权限；
- 需要由工程师承担的最终设计责任。

AI Agent 的工作应当具有以下特征：

```text
先理解，再行动
先划定边界，再修改
先建立依据，再解释
先验证，再下结论
保留失败，再汇总结果
记录变化，支持回退
```

---

## 13. 参考样例与专项内容

项目可以提供参考样例，但参考样例的作用是解释基础设施如何使用，而不是替使用者完成实际项目。

参考样例应当：

- 规模适当；
- 边界清楚；
- 易于阅读；
- 能够说明接入方式；
- 明确说明假设和限制；
- 不冒充通用工程结论。

具体电路、具体控制器、具体实验、具体参数和具体算法研究，应当作为独立的专项内容管理。专项内容可以丰富，但不能反向污染基础设施的抽象边界。

---

## 14. 工程建设阶段

为了实现上述目标，后续建设可以按以下阶段组织：

### 阶段 A：基础目标与边界

明确项目定位、适用范围、不适用范围、责任边界和公开内容边界。

### 阶段 B：基础设施范式

建立能够承载模型、电路、控制器、实验和结果的公共组织方式。此阶段关注接口思想、职责划分和可扩展性，不追求完成某个具体算法。

### 阶段 C：通用运行与证据能力

建立一致的运行秩序、数据保存方式、版本识别方式、结果导出方式和审计思路。

### 阶段 D：参考接入样例

选择少量简单内容，说明使用者和 AI Agent 如何在基础设施上接入自己的模型、电路和控制器。

### 阶段 E：专项方法示例

以相互独立的参考内容展示反馈、前馈、事件型控制、优化方法、学习方法或神经网络方法的接入思路。

### 阶段 F：文档、工具与发布治理

完善使用说明、AI 协作规范、数据和可视化工具说明、版本与 Hash 机制说明、发布边界和维护方式。

这些阶段是建设顺序，不是要求每个使用者都完整重复的实验步骤。

---

## 15. 最终架构判断

本项目的核心不是某个具体仿真结果，而是一套能够长期承载不同研究工作的工程范式：

```text
明确的目的
  + 稳定的基础设施
  + 清晰的职责边界
  + 可扩展的运行方式
  + 独立的专项研究
  + 可追溯的结果体系
  + 受约束的 AI 协作
```

可以将其概括为：

```text
基础设施规定如何承载仿真
运行框架规定如何组织一次运行
专项内容规定使用者研究什么
证据体系规定结果能够说明什么
AI Agent 协作规范规定如何安全地共同完成工作
```

后续具体技术说明、接口定义、数据格式、Hash 工具、导出工具、可视化工具、参考电路和算法样例，应当分别放入相应的专题文档和参考目录中，不再堆积到本文。

本项目始终保持以下根本边界：

```text
这是仿真基础设施和 AI 协作范式
不是具体产品的自动设计系统
不是某种算法的最终实现
不是硬件安全或产品认证依据
```
